import 'package:flutter/material.dart';

class DependenciesUpgradeScreen extends StatelessWidget {
  const DependenciesUpgradeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Upgrades'),
    );
  }
}
