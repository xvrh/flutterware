include: ../../analysis_options.yaml

analyzer:
  exclude:
    - build/**
    # A scene file is ordinary Dart and is analyzed like any other. Nothing
    # is excluded any more.
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
