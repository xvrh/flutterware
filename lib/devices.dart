/// The device vocabulary the GUI's pickers, `fw`, MCP and the scenario axes
/// all speak — `Devices.iphone16`, and `Device(...)` for a screen the table
/// does not have.
///
/// Re-exported by `plugins.dart` (for a project's `tool/flutterware.dart`) and
/// by `flutter_test.dart` (for a scenario folder's `flutter_test_config.dart`),
/// so neither has to import this library by name.
library;

export 'src/devices.dart';
