name: flutterware_example
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 2.0.0+1

environment:
  # 3.10 for the dot shorthands `tool/flutterware.dart` uses.
  sdk: ^3.13.0-0

resolution: workspace

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  android_id:
  auto_size_text:
  collection:
  device_info_plus:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage:
  flutterware:
    path: ../..
  # For lib/network_spike.dart — the http-profile spike's traffic generator.
  http:
  logging:
  os_detect:
  package_info_plus:
  path:
  path_provider:
  # For lib/src/devbar/permission_adapter.dart — the permissions-panel
  # dogfood. flutterware itself never depends on this; the adapter exists so
  # the app can bring whichever package it already uses.
  # Pinned below 13: permission_handler_android 14 requires Android SDK 37,
  # which this machine's SDK does not have. Nothing in the adapter depends on
  # the version — it maps six PermissionStatus values, and those are stable.
  permission_handler: ^12.0.0
  shared_preferences:
  # For bin/example_server.dart — the server-inspection example.
  shelf:
  # For lib/src/database/app_database.dart — the db-panel dogfood.
  sqlite3_flutter_libs:
  sqlite_async:

dev_dependencies:
  flutter_lints:
  flutter_native_splash: ^2.4.0
  flutter_test:
    sdk: flutter
  lints:
  test:
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Fixtures for the asset inspector — see assets/README.md for what each one
  # is meant to expose. Several of these are wrong on purpose.
  assets:
    - assets/images/
    - assets/icons/check.svg
    - path: assets/icons/heart.svg
    - assets/animations/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  fonts:
    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto-Regular.ttf
        - asset: assets/fonts/Roboto-Bold.ttf
          weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
